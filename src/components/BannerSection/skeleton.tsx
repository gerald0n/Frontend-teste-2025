import { SkeletonContainer, SkeletonOverlay } from './style'

const SkeletonBanner = () => {
  return (
    <SkeletonContainer>
      <SkeletonOverlay />
    </SkeletonContainer>
  )
}

export default SkeletonBanner
