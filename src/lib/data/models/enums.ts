export enum BannerType {
  image = 'image',
  video = 'video',
  other = 'other',
}

export enum BannerTarget {
  self = '_self',
  blank = '_blank',
}

export enum BannerStatus {
  published = 'published',
  draft = 'draft',
}
